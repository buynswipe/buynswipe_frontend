"use client"
import { useState } from "react"

export function NewOfferForm() {
  const [message, setMessage] = useState("")
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setMessage("Saving…"); const body = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/admin/affiliates", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); setMessage(response.ok ? "Offer saved" : "Unable to save offer"); if (response.ok) event.currentTarget.reset() }
  return <form onSubmit={submit} className="grid gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-5 sm:grid-cols-2"><input name="name" required placeholder="Offer name" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="partner" required placeholder="Partner" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="category" required placeholder="Category" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="destinationUrl" required type="url" placeholder="Destination URL" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="trackingUrl" type="url" placeholder="Tracking URL (optional)" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><textarea name="disclosure" required placeholder="Disclosure text" className="min-h-20 rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm sm:col-span-2" /><div className="flex items-center gap-3 sm:col-span-2"><button className="rounded-lg bg-slate-900 px-4 py-2 text-sm font-bold text-white">Save draft</button><span className="text-xs text-slate-500" role="status">{message}</span></div></form>
}
