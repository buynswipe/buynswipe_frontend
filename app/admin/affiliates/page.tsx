import Link from "next/link"
import type { ReactNode } from "react"
import { ArrowLeft, ExternalLink, Link2, ShieldCheck } from "lucide-react"
export default function AdminAffiliatesPage() { return <main className="min-h-screen bg-slate-950 text-slate-950"><div className="mx-auto min-h-screen max-w-7xl bg-slate-50 p-6 lg:p-10"><Link href="/admin" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-sky-600"><ArrowLeft className="size-4" /> Admin overview</Link><header className="mt-8"><p className="text-sm font-semibold text-violet-600">Partner operations</p><h1 className="mt-2 text-3xl font-black">Affiliate operations</h1><p className="mt-2 text-sm text-slate-500">Manage offer records, destinations, disclosures, and link health from one queue.</p></header><div className="mt-8 grid gap-4 md:grid-cols-3"><Panel icon={<Link2 />} title="Offer catalog" text="Structured product offers, partner ownership, commission metadata, and activation status." /><Panel icon={<ExternalLink />} title="Link health" text="Recheck redirects, final destinations, HTTP status, and duplicate URLs." /><Panel icon={<ShieldCheck />} title="Disclosure review" text="Keep affiliate disclosures and evidence attached to every public offer." /></div></div></main> }
function Panel({ icon, title, text }: { icon: ReactNode; title: string; text: string }) { return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid size-11 place-items-center rounded-xl bg-violet-100 text-violet-700">{icon}</div><h2 className="mt-5 font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></section> }
