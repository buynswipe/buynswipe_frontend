import Link from "next/link"
import type { ReactNode } from "react"
import { ArrowLeft, BadgeIndianRupee, ShieldCheck } from "lucide-react"
export default function AdminCreditPage() { return <main className="min-h-screen bg-slate-950 text-slate-950"><div className="mx-auto min-h-screen max-w-7xl bg-slate-50 p-6 lg:p-10"><Link href="/admin" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-sky-600"><ArrowLeft className="size-4" /> Admin overview</Link><header className="mt-8"><p className="text-sm font-semibold text-emerald-600">Product governance</p><h1 className="mt-2 text-3xl font-black">Credit catalog</h1><p className="mt-2 text-sm text-slate-500">The next module will centralize credit products, claims, rates, and disclosures.</p></header><div className="mt-8 grid gap-4 md:grid-cols-2"><Panel icon={<BadgeIndianRupee />} title="Products and claims" text="Create structured loan and card records with ownership, source evidence, review dates, and publishing states." /><Panel icon={<ShieldCheck />} title="Compliance queue" text="Review rate language, eligibility claims, affiliate disclosures, and stale content before publishing." /></div></div></main> }
function Panel({ icon, title, text }: { icon: ReactNode; title: string; text: string }) { return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid size-11 place-items-center rounded-xl bg-emerald-100 text-emerald-700">{icon}</div><h2 className="mt-5 font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></section> }
