"use client"
import { useState } from "react"

export function NewProductForm() {
  const [message, setMessage] = useState("")
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setMessage("Saving…"); const body = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/admin/credit", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); setMessage(response.ok ? "Product saved" : "Unable to save product"); if (response.ok) event.currentTarget.reset() }
  return <form onSubmit={submit} className="grid gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-5 sm:grid-cols-2"><input name="name" required placeholder="Product name" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="provider" required placeholder="Provider" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="productType" required placeholder="Product type" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="rateText" placeholder="Rate / APR" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="feeText" placeholder="Fee details" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><input name="sourceUrl" type="url" placeholder="Evidence URL" className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" /><textarea name="eligibility" placeholder="Eligibility notes" className="min-h-20 rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm sm:col-span-2" /><div className="flex items-center gap-3 sm:col-span-2"><button className="rounded-lg bg-slate-900 px-4 py-2 text-sm font-bold text-white">Save draft</button><span className="text-xs text-slate-500" role="status">{message}</span></div></form>
}
