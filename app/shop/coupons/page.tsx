import type { Metadata } from "next"
import Link from "next/link"
import { Check, Copy, ShieldCheck } from "lucide-react"
import { coupons } from "@/lib/shop-data"
import { CouponReportForm } from "@/app/shop/coupon-report-form"

export const metadata: Metadata = { title: "Verified Coupons | ShopNdeal", description: "Browse transparent ShopNdeal coupon offers with verification and expiry details." }
export default function CouponsPage() { return <main className="min-h-screen bg-[#f7f9fc] px-5 py-10"><div className="mx-auto max-w-5xl"><Link href="/shop" className="text-sm font-bold text-blue-600">← Back to ShopNdeal</Link><h1 className="mt-8 text-4xl font-black tracking-tight text-slate-950">Coupons with a confidence signal.</h1><p className="mt-3 max-w-2xl text-slate-500">Every coupon shows its expiry, minimum order and last known verification state. Demo catalog data is clearly marked until live merchant integrations are connected.</p><div className="mt-8 space-y-4">{coupons.map((coupon) => <article key={coupon.code} className="flex flex-col gap-5 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><p className="text-xs font-black uppercase tracking-wider text-blue-600">{coupon.merchant}</p><span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-2 py-1 text-[11px] font-bold text-emerald-700"><ShieldCheck className="size-3" /> {coupon.status}</span></div><h2 className="mt-2 text-lg font-black text-slate-950">{coupon.offer}</h2><p className="mt-1 text-sm text-slate-500">Minimum {coupon.minimum} · Expires {coupon.expiry}</p></div><div className="flex flex-col items-end gap-2"><div className="flex items-center gap-3"><code className="rounded-xl border border-dashed border-slate-300 bg-slate-50 px-3 py-2 text-sm font-black text-slate-800">{coupon.code}</code><button type="button" className="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-3 py-2 text-xs font-bold text-white"><Copy className="size-3.5" /> Copy</button></div><CouponReportForm code={coupon.code} merchant={coupon.merchant} /></div></article>)}</div><div className="mt-8 flex items-center gap-2 rounded-2xl bg-blue-50 p-4 text-sm text-blue-800"><Check className="size-4" /> Coupon success and eligibility can change at checkout. Verify terms before paying.</div></div></main> }
