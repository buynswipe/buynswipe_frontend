import type { Metadata } from "next"
import Link from "next/link"
import { ArrowRight, ShieldCheck } from "lucide-react"
import { formatINR, shopItems } from "@/lib/shop-data"

export const metadata: Metadata = { title: "Deals | ShopNdeal", description: "Explore current ShopNdeal deal starting points across shopping categories." }
export default function DealsPage() { return <main className="min-h-screen bg-slate-950 px-5 py-10 text-white"><div className="mx-auto max-w-7xl"><Link href="/shop" className="text-sm font-bold text-cyan-300">← Back to ShopNdeal</Link><div className="mt-10 max-w-2xl"><p className="text-sm font-bold uppercase tracking-[.16em] text-cyan-300">Today&apos;s starting points</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-5xl">Deals that show their working.</h1><p className="mt-4 leading-7 text-slate-300">Compare the price, discount and cashback route before you click through. Values are indicative until live eligibility is confirmed.</p></div><div className="mt-10 grid gap-5 md:grid-cols-2">{shopItems.map((item) => <Link key={item.slug} href={`/shop/product/${item.slug}`} className="group rounded-3xl border border-white/10 bg-white/[.07] p-5 hover:bg-white/[.11]"><div className="flex items-start justify-between gap-4"><div><span className="rounded-full bg-cyan-300 px-2.5 py-1 text-xs font-black text-slate-950">{item.discount}% OFF</span><h2 className="mt-5 text-xl font-black">{item.name}</h2><p className="mt-1 text-sm text-slate-400">{item.merchant} · {item.category}</p></div><ShieldCheck className="size-5 text-emerald-300" /></div><div className="mt-8 flex items-end justify-between"><div><p className="text-2xl font-black">{formatINR(item.price)}</p><p className="text-sm text-slate-400 line-through">{formatINR(item.mrp)}</p></div><span className="inline-flex items-center gap-2 text-sm font-bold text-cyan-300 group-hover:gap-3">Compare savings <ArrowRight className="size-4" /></span></div></Link>)}</div></div></main> }
