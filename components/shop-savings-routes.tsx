import Link from "next/link"
import { ArrowUpRight, Check, ShieldCheck } from "lucide-react"
import { formatINR } from "@/lib/shop-data"
import type { SavingsRoute } from "@/lib/shop/savings-routes"

export function ShopSavingsRoutes({ routes, outboundHref }: { routes: SavingsRoute[]; outboundHref: string }) {
  return <section className="mt-8" aria-labelledby="compare-savings-heading"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-cyan-700">Savings intelligence</p><h2 id="compare-savings-heading" className="mt-2 text-2xl font-black tracking-tight">Compare all ways to save</h2></div><span className="hidden rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-black text-emerald-700 sm:inline-flex"><ShieldCheck className="mr-1.5 size-3.5" /> Demo catalog routes</span></div><div className="mt-5 grid gap-4 lg:grid-cols-3">{routes.map((route, index) => <article key={route.id} className={`rounded-2xl border p-5 ${index === 0 ? "border-cyan-200 bg-cyan-50/70" : "border-slate-200 bg-white"}`}><div className="flex items-start justify-between gap-3"><h3 className="font-black">{route.label}</h3>{index === 0 && <span className="rounded-full bg-white px-2 py-1 text-[10px] font-black uppercase text-cyan-700">Lowest pay now</span>}</div><dl className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-slate-500">Pay now</dt><dd className="mt-1 text-xl font-black">{formatINR(route.payNow)}</dd></div><div><dt className="text-slate-500">Potential cashback</dt><dd className="mt-1 font-black text-emerald-700">{formatINR(route.cashback)}</dd></div></dl><div className="mt-4 border-t border-slate-200/80 pt-3"><p className="text-xs text-slate-500">Potential net cost</p><p className="mt-1 text-lg font-black">{formatINR(route.potentialNetCost)}</p><p className="mt-3 flex gap-2 text-xs font-semibold text-slate-600"><Check className="size-4 shrink-0 text-emerald-600" />{route.explanation}</p></div>{route.offerIds.length > 0 && <Link href={outboundHref} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-black text-white hover:bg-blue-700">Shop this route <ArrowUpRight className="size-4" /></Link>}</article>)}</div><p className="mt-4 text-xs leading-5 text-slate-500">Demo catalog routes are for product comparison only. Potential cashback is separate from the amount paid now and depends on the offer terms. ShopNdeal does not treat potential cashback as guaranteed savings.</p></section>
}
